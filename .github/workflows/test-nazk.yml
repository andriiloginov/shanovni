name: Test NAZK API access

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test NAZK API
        run: |
          curl -s -o response.json -w "%{http_code}" \
            "https://public-api.nazk.gov.ua/v2/documents/list?query=Klichko"
